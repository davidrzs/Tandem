name: CI

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11.8.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create .env
        run: |
          cp .env.example .env
          secret="$(openssl rand -base64 32)"
          sed -i "s|^BETTER_AUTH_SECRET=.*|BETTER_AUTH_SECRET=${secret}|" .env

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm -r typecheck

      - name: Unit + integration tests
        run: pnpm --filter @tandem/editor --filter @tandem/core --filter @tandem/server test

      - name: Web build
        run: pnpm --filter @tandem/web build

      - name: Install Playwright browser
        run: pnpm exec playwright install --with-deps chromium

      - name: End-to-end tests
        run: bash apps/web/e2e/run-all.sh

      # The Playwright journey suite boots its own server on :3799 against the
      # built SPA. Running it here keeps its assertions from rotting against UI
      # changes (it isn't exercised anywhere else).
      - name: Playwright journeys
        run: pnpm e2e

      - name: Upload e2e logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-logs
          path: |
            /tmp/rt-http.log
            /tmp/rt-vite.log
            /tmp/rt-migrate.log
          if-no-files-found: ignore

  docker:
    # The image self-hosters run: prove it still builds on every change.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Build production image
        run: docker build -t tandem:ci .
